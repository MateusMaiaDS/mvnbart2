# bart_mod <- dbarts::bart(x.train = x_train,y.train = c)
# bart_mod$sigma %>% mean
#
# plot(bart_mod$yhat.train.mean,c)
# plot(rowMeans(c_train_post[,1001:1500]),c_train)
# plot(bart_mod$yhat.train.mean,rowMeans(c_train_post[,1001:1500]))
#
# crossprod(bart_mod$yhat.train.mean,c)
# crossprod(rowMeans(c_train_post[,1001:1500]),c_train)
#
# all_rho %>% mean
# plot(bart_mod$yhat.train.mean,)
# all_tau_c_post^(-1/2) %>% mean
# all_tau_q_post^(-1/2) %>% mean
#
